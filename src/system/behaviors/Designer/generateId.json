{
	"component": "Designer",
	"state": "generateId",
	"action": "function generateId() {\n  function gen() {\n      return Math.floor((1 + Math.random()) * 0x10000).toString(16);\n  }\n  return gen() + gen() + gen();\n}"
}